# 0.3.10.2.41 — UI_LibraryRefreshHotfix

## Motivo
Al iniciar la app en 0.3.10.2.40, el `LibraryPanel` fallaba con:

- `AttributeError: 'LibraryPanel' object has no attribute '_on_refresh_clicked'`

Esto dejaba la UI inutilizable.

## Cambios
- Se implementa `LibraryPanel._on_refresh_clicked()` (🔄 Refrescar).
  - Re-escanea el filesystem de componentes.
  - Reconstruye árbol.
  - Recarga miniaturas.
  - Best-effort: intenta mantener carpeta/archivo seleccionado.
- Fix colateral: el filtro de búsqueda ahora fuerza recarga de miniaturas del folder actual.
  - Antes: `_load_folder()` retornaba early si era el mismo dir → no re-aplicaba filtro en thumbs.

## Archivos tocados
- `rcs/ui/library_panel.py`
- `rcs/core/version.py`
- `ai/context.json`
- `docs/CHANGELOG.md`
- `docs/patches/index.md`

## DoD
- `python -m rcs.app` abre sin crash.
- Botón 🔄 funciona y no rompe selección/carpeta en casos normales.
- Buscar filtra árbol + miniaturas (thumbs) de forma consistente.
