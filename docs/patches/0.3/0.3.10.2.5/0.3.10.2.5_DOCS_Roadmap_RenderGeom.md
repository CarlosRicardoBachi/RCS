# 0.3.10.2.5 — DOCS — Roadmap Render/Geom (Skia + svgelements)

## Objetivo

Formalizar el plan **Render/Geometría** en pasos chicos y verificables, para atacar:
- render “invisible” (canvas en blanco / rectángulo vacío),
- divergencia canvas vs thumbnails,
- invalidación de cache (stale thumbs/canvas),
sin tocar funcionalidad de la app.

## Archivos tocados

- `docs/RCS_STATUS.md` — prioridad actual: **Render/Geom**; Bloque Texto pasa a backlog.
- `docs/RCS_ROADMAP_RENDER_GEOM.md` — roadmap en fases 0..4 con entregables tipo ZIP, cada uno con **DoD + checklist**.
- `docs/OBSTACLES.md` — agrega/estructura OBS-008/009/010 (render invisible, divergencia, cache).
- `docs/patches/index.md` — agrega entrada del patch.
- `docs/CHANGELOG.md` — agrega entrada del patch.
- `ai/context.json` — versión/foco actualizado para que la IA trabaje *en el problema correcto*.

## Notas

- **No se toca código** en este parche.
- `docs/metadata` no está presente en este ZIP base; aun así este parche es solo de documentación.

## Quick check (30s)

1. Abrir `docs/RCS_ROADMAP_RENDER_GEOM.md` y verificar que:
   - Fases 0..4 existen,
   - cada entregable tiene **DoD** (checkboxes) + **Checklist** (pasos).
2. Abrir `docs/RCS_STATUS.md` y confirmar:
   - prioridad = Render/Geom,
   - Texto marcado como backlog.
3. Abrir `docs/OBSTACLES.md` y confirmar que existen:
   - OBS-008, OBS-009, OBS-010.
