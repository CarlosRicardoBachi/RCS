# Checklist 0.3.10.2.74 — HOTFIX GMPR import + SVG preview

1) **Arranque**
- `python -m rcs.app` abre sin tracebacks.
- En el log debe decir `v0.3.10.2.74`.

2) **Insertar SVG desde biblioteca**
- Abrir panel Biblioteca → doble click en un SVG.
- No debe aparecer `AttributeError ... _render_svg_qt`.
- Debe verse el SVG en el lienzo.

3) **Importar GMPR con SVG embebido + raster**
- Archivo → Importar GMPR → seleccionar un GMPR que tenga `svg_embedded`.
- Debe verse el **preview del SVG base** (fondo/guía).
- Debe aparecer el raster.

4) **Alineación**
- El raster debe estar en coordenadas coherentes respecto del SVG base (no “tirado” fuera del lienzo).
- Probar mover/escala/rotar raster y guardar/exportar si aplica.

5) **Regresión**
- Abrir un SVG normal (no GMPR) y operar: zoom/pan, seleccionar, borrar.
