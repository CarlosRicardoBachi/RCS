# Checklist — 0.3.10.2.69 HOTFIX GMPR Base SVG (preview a escala + selección exclusiva)

- [ ] `python -m rcs.app` muestra `v0.3.10.2.69`.
- [ ] Importar un GMPR con SVG + raster:
  - [ ] El SVG base se ve en escala real (cubre el lienzo esperado).
  - [ ] Raster queda alineado con el SVG (posición/tamaño correctos).
- [ ] Click en zona vacía selecciona el SVG base (inspección).
- [ ] Al seleccionar cualquier objeto real (raster/svg objeto), el SVG base NO queda seleccionado junto a ellos.
- [ ] No hay crash al importar GMPR.
- [ ] Guardar proyecto RCS sigue funcionando.
